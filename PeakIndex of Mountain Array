#include <iostream>
#include <vector>

using namespace std;

int PeakIndex(vector<int> nums,  int n)
{

    int st = 0;
    int end = n - 1;

    while (st <= end)
    {

        int mid = st + (end - st) / 2;

        if (nums[mid-1] < nums[mid] && nums[mid] > nums[mid + 1])
        {

            return mid;
        }
        if(nums[mid]<nums[mid+1])
        {
            st = mid + 1;
        }
        else
        {
            end = mid - 1;
        }
       
    }

    return -1;
}

int main()
{

    int n, tar;

    cout << "Enter the number of elements: ";
    cin >> n;

    

    vector<int> nums;

    for (int i = 0; i < n; i++)
    {

        int x;

        cout << "Enter the value of index " << i << " :";
        cin >> x;

        nums.push_back(x);
    }

    int index = PeakIndex(nums,  n);

    if (index != -1)
    {

        cout << "The index of target is : " << index;
    }
    else
    {
        cout << "Target not found !!";
    }
    return 0;
}
